name: cached_network_image_widget
description: A Flutter package providing advanced CachedNetworkImage and SVG image caching with BlurHash placeholders and shimmer effects.
version: 1.0.0
homepage: https://github.com/safeenkurd/cached_network_image_widget
repository: https://github.com/safeenkurd/cached_network_image_widget
issue_tracker: https://github.com/safeenkurd/cached_network_image_widget/issues
license: MIT

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.4.1
  cached_network_svg_image: ^1.2.0
  flutter_blurhash: ^0.9.1
  flutter_cache_manager: ^3.4.1
  flutter_animate: ^4.5.2
